/**
 * Palettes for the Lava World planet type.
 *
 * Three layers:
 *   - land surface (rock shader, 3 colors)
 *   - impact craters (craters shader, 2 colors)
 *   - glowing lava channels (lava-rivers shader, 3 colors — transparent except at cracks)
 * Original: LavaWorld.tscn.
 */
import type { PaletteDef } from "./types";
declare const palettes: PaletteDef[];
export default palettes;
