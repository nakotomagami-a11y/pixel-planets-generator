/**
 * Palettes for the Islands planet type.
 *
 * Three layers:
 *   - ocean (rock shader, 3 colors — solid sphere fill)
 *   - land masses (landmass shader, 4 colors — partially transparent overlay)
 *   - cloud layer (gas shader, 4 colors)
 * Original: LandMasses.tscn.
 */
import type { PaletteDef } from "./types";
declare const palettes: PaletteDef[];
export default palettes;
