/**
 * React bindings for @agent-office/pixel-planets.
 *
 * Import from "@agent-office/pixel-planets/react".
 *
 * Note for Next.js / RSC users: this component uses `useEffect` and `useRef`
 * (client-only React APIs). Wrap it in a thin "use client" boundary:
 *
 *   "use client";
 *   export { PlanetCanvas } from "@agent-office/pixel-planets/react";
 */

export { PlanetCanvas } from "./PlanetCanvas";
export type { PlanetCanvasProps } from "./PlanetCanvas";
